function AddNewUser(props) {
    return (
        <>
            <button className="btn-add btn" onClick={props.onClick}>Add new user</button>{/* 229 ред в App.jsx */}
        </>
    )
}

export default AddNewUser;